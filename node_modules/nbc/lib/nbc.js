var application = require('./application'),
    connect = require('connect'),
    utils = connect.utils;

createApplication = function() {
    var app = connect();
    utils.merge(app, new application());
    return app;
};

exports = module.exports = createApplication;